<div id='records'>
  <% if searching? %>
    <h2><%= t('shared.admin.search.results_for', :query => params[:search]) %></h2>
  <% end %>
  <% unless @entry_attributes.nil? %>
    <%= render :partial => 'list', :collection => @entry_attributes %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_attributes_yet') %>
        </strong>
      <% else %>
        <%= t('shared.admin.search.no_results') %>
      <% end %>
    </p>
  <% end %>
</div>
<div id='actions'>
  <ul>
    <li>
      <%= render :partial => "/shared/admin/search",
                 :locals => {:url => admin_catalog_type_catalog_entry_entry_attributes_url(@catalog_type, @catalog_entry) } %>
    </li>
  </ul>
</div>

