<div id='records'>
  <% if searching? %>
    <h2><%= t('shared.admin.search.results_for', :query => params[:search]) %></h2>
  <% end %>
  <% if @catalog_types.any? %>
    <%= render :partial => 'list', :collection => @catalog_types %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_catalog_types_yet') %>
        </strong>
      <% else %>
        <%= t('shared.admin.search.no_results') %>
      <% end %>
    </p>
  <% end %>
</div>
<div id='actions'>
  <ul>
    <li>
      <%= render :partial => "/shared/admin/search",
                 :locals => {:url => admin_catalog_types_url} %>
    </li>
    <li>
      <%= link_to t('.create_new_catalog_entry'), new_admin_catalog_type_url,
                  :class => "add_icon" %>
    </li>
  </ul>
</div>

