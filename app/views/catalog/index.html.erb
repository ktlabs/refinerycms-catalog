<% content_for :javascript_libraries, jquery_include_tags(:jquery_ui => true) %>

<% content_for :body_content_title do -%>
  Catalogs
<% end -%>

<% content_for :body_content_left do -%>
  <!--<% @catalog_entries.each do | catalog | -%>
    <span>
      <%= link_to raw("#{image_fu catalog.title_image, '200x200' unless catalog.title_image.nil?} #{catalog.title}"),
                  catalog_path(catalog) %>
    </span>
  <% end -%> -->
  <div>
    <div><script>alert($.tabs);</script></div>
  </div>
  
<% end -%>

<%= render :partial => '/shared/content_page' -%>

